# 2026 首届 openvela AI 硬件开发者大赛 · 技术报告

> 状态：**草稿**。已定稿 摘要 / 3.1 / 3.2 / 3.3 / 3.4；
> 待补 1 信息表 / 3.5 测试 / 3.6 AI-Native / 3.7 总结。

---

## 2、摘要

桌面焊接常需充电器、焊台、功率计三机并用，线缆零散、状态割裂，烙铁空烧无人提醒。本作品基于
openvela 在 STM32H750B-DK 上整合为一体化焊接工作站：以 TCA9548A 扩展 4 颗同地址 SW3538 实现
四路独立快充，2 颗 INA226 计量整机输入与加热丝功率，并自研基于 NuttX i2c_bitbang 的软件 I2C
传输层；温控侧以 MAX6675 与 PID 闭环驱动 JBC245，并将软件 PWM 由 10 档加密至 20 档，避免恒温
工作点被量化丢失。整机接入 ai_agent 提供对话与主动提醒，应用代码 4133 行、编译零告警，沉淀
焊接会话 Skill 1 个；开发板于验证阶段损坏，实测数据缺失，报告中已如实标注。

---

## 3、正文

### 3.1 绪论

#### 3.1.1 项目背景与问题定义

桌面电子焊接场景通常需要三类设备并存：为手机与开发板供电的多口快充充电器、用于焊接作业的
恒温焊台、以及测量实际功耗的外接功率计。三者各自独立——每个设备占用一个插座位与一组线缆，
本就有限的桌面空间被切碎；数据彼此隔离，想知道烙铁此刻实际输出多少瓦必须外接功率计，想知道
充电器协商到了什么协议、跑在多少瓦只能靠指示灯猜测。更关键的是，设备之间没有任何信息交换
能力，无法形成安全联动。

其中最突出的是焊接安全问题。恒温焊台离开烙铁架后持续加热，而"焊完忘记关"在个人用户的日常
操作中是常态：轻则烙铁头长期空烧氧化报废，重则引发火灾。现有焊台产品最多提供本机定时休眠，
无法跨越设备边界向用户发出提醒——因为焊台自身不具备感知用户、理解意图并主动触达的能力。

将上述设备整合到一块主控上，也就意味着把三条独立的信号链并入同一套总线与同一块屏幕。受限于
开发板既有的外设布局——硬件 I2C 已被触摸屏占用，且充电芯片采用固定地址——整合过程需要在
总线资源与器件寻址上重新规划。

#### 3.1.2 创新点

**（1）面向焊接安全的主动式 Agent —— 场景创新**

作品将大模型 Agent 引入焊接工作流，使设备从"被动等待操作"转为"主动感知风险"。运行于设备端的
ai_agent 通过自定义 Skill 记忆用户的常用温度，在作业开始时主动建议设置定时巡检，并在预定时刻
推送提醒——把"人盯着设备"变成"设备盯着人"。该能力建立在设备本机 AI 链路上，不依赖云端轮询。

**（2）多口快充的完全透明化 —— 交互创新**

四组快充通道（4×Type-C + 4×Type-A）在传统充电器上仅以指示灯表示通断。本作品为每一路单独呈现
实时电压、电流、功率与协商到的快充协议，并叠加整机输入功率，使功率分配过程对用户完全可见。
这一呈现要求同时解决多路数据的同屏组织与逐路寻址问题。

---

### 3.2 系统方案设计

#### 3.2.1 开发板选型与资源分配

本作品的主控为大赛指定的 STM32H750B-DK。该板的核心条件与本作品的需求高度契合，因此选型本身
不构成决策点，真正的工作在于如何在板上重新分配既有资源。

**算力与内存**：STM32H750 为 Cortex-M7 内核，配合 8 MB SDRAM 与 LTDC 显示控制器，可同时承载
480×272 的 LVGL 界面渲染与实时的温控闭环运算，无需在显示效果与控制精度之间取舍。

**外设资源**：板载以太网 PHY（LAN8740A）为 ai_agent 提供了原生网络通道，无需额外扩展模块即可
接入云端大模型；多路 GPIO 与 I2C 则为传感器与执行器留出了接口空间。

**资源冲突**：板载硬件 I2C4 已被触摸屏占用，且本作品所需的四颗快充芯片采用固定地址 0x3C。
这两点约束直接决定了后续方案在总线选择与器件寻址上的设计走向。

#### 3.2.2 系统总体架构

```
┌──────────────────────── STM32H750B-DK (Cortex-M7) ────────────────────────┐
│   ┌──────────────┐          ┌──────────────────────────────────────┐     │
│   │  LVGL 9.1    │◄────────►│      charging_station  主应用         │     │
│   │  三页触控界面 │          │  (100ms 定时调度)                     │     │
│   └──────┬───────┘          └──────┬───────────────────────────────┘     │
│          │ 触摸                   ▼ 驱动层                                │
│   ┌──────┴───────┐          ┌──────────────────────────────────────┐     │
│   │ FT5x06       │          │ iron.c     MAX6675 + PID + 软件PWM    │     │
│   │ 硬件 I2C4    │          │ sw3538.c   4 路快充寄存器             │     │
│   └──────────────┘          │ ina226.c   2 路功率计量               │     │
│                             │ cs_i2c_sw.c 软件 I2C 传输层           │     │
│   ┌──────────────┐          └──────────────┬───────────────────────┘     │
│   │  ai_agent    │                         │                              │
│   │  LLM 对话/Skill│                        │ GPIO D2~D9                   │
│   └──────┬───────┘                         ▼                              │
│          │ LAN8740A                 ┌──────────────┐                     │
│          │ 以太网                    │ TCA9548A     │                     │
└──────────┼──────────────────────────┴──────┬───────┴─────────────────────┘
           ▼                                 │
   ┌───────────────┐          ┌──────────────┴──────────────┐
   │  DeepSeek API │          │ CH0~CH3  SW3538 ×4  (0x3C)  │
   │   (云端 LLM)   │          │ CH4      INA226 整机输入     │
   └───────────────┘          │ CH5      INA226 加热丝       │
                              └─────────────────────────────┘
```

*（ASCII 示意，转 docx 时建议重绘为标准框图）*

#### 3.2.3 端侧与云端的职责划分

本作品的职责边界划分遵循一条原则：**安全相关的闭环全部留在端侧，云端只负责增强**。

| 功能层 | 运行位置 | 断网时表现 |
|---|---|---|
| 四路快充管理 | 端侧 | 不受影响 |
| 烙铁温控（PID + PWM） | 端侧 | 不受影响 |
| 触控界面与状态显示 | 端侧 | 不受影响 |
| AI 对话 | 云端 LLM | 不可用 |
| Skill 定时提醒 | 端侧触发 | 触发正常，投递依通道而定 |

这一划分的出发点是：充电与温控属于用户正在依赖的基础功能，其可用性不能取决于网络状况。云端
大模型的价值在于自然语言理解与上下文推理，属于能力增强而非功能前提。因此断网时，设备退化为
一台功能完整但无对话能力的充电站，而非不可用设备。

---

### 3.3 核心算法与技术原理

#### 3.3.1 AI 能力落位

本作品的 AI 能力以云端大模型形式落地，未使用端侧推理模型。这一选择基于需求本身：充电管理与
温度控制均为确定性闭环算法（状态量明确、控制律可解析），引入神经网络既无必要，也会占用本就
紧张的算力与内存预算（系统堆约 940 KB）。端侧承担感知与执行，云端承担语义理解与意图解析，
形成明确的职责边界。

**云端模型**：设备通过板载 LAN8740A 以太网 PHY 接入网络，由 ai_agent 框架完成 HTTPS 链路与
对话管理。鉴权凭据（API Key）存放于设备端数据目录，由框架在发起请求时注入。

**链路层工作**：打通该链路的过程中定位并修复了三处框架缺陷——显存与系统堆地址重叠导致的启动
崩溃、HTTP chunked 响应读取不退出导致的超时、以及计时基准受系统时钟跳变影响引发的看门狗误判。
三者为连锁问题，修复其一才暴露其二。

#### 3.3.2 关键机制设计

**（1）温度闭环控制**

```
                    ┌─────────────┐
    目标温度 ──►(−)──┤  PID 控制器  ├──► 输出限幅 ──► 20 档软件 PWM ──► 加热
              ▲     └─────────────┘        0~100%         (2s 周期)
              │                                     │
              └──────── MAX6675 热电偶 ◄────────────┘
                        (300ms 采样)
```

采样节拍：MAX6675 单次转换最长 220 ms，因此每 3 个 tick（300 ms）读取一次，留有 36% 余量；
温度超过 500 °C 判为异常，立即切断加热并将传感器标记为失效。

抗积分饱和（条件积分）：当输出已达限幅且误差方向不变时停止积分累加，避免退饱和时的长时间超调。

微分抑制（斜率限幅）：温度每 300 ms 采样一次，原始差商噪声大，因此对实测变化率限幅至 ±8 °C/s，
而非对输出项限幅。

**参数口径说明**：UI 暴露给用户的 P/I/D 与控制器实际使用的增益存在固定缩放（P×0.2、I×0.4、
D×0.2），目的是让用户界面上的数字更符合直觉。报告及代码注释中出现的增益均需标明口径。

**（2）PWM 量化误差分析**

问题：JBC245 烙铁额定功率 84 W（24 V × 3.5 A），而空烧恒温（不进行焊接）时仅需约 5~15 W 维持，
对应占空比 6% ~ 18%。

10 档 PWM 的失效——分辨率 10% 意味着该区间只能取两个值：

| PID 输出 | 10 档下的实际占空比 |
|---|---|
| 1% ~ 9% | 0%（完全断供） |
| 10% ~ 19% | 10% |

温度到达设定值后 PID 输出回落至该区间，加热器在"断供"与"欠供"之间跳变，形成低频极限环，温度
持续小幅振荡而无法稳定。

解决：将 PWM 档位由 10 档加密至 20 档（周期 1 s → 2 s，分辨率 5%），并以四舍五入替代截断取整。
改进后该工作区间有 5% / 10% / 15% / 20% 四档可用，恒温得以稳定。

周期延长的合理性：JBC245 烙铁头的热时间常数在 10~30 s 量级，2 s 的调制周期远小于其热惯性，
功率脉动不会反映到温度上。

**（3）安全联动机制**

层级一：端侧硬保护（不依赖任何外部条件）

- 休眠检测：JBC245 手柄放入烙铁架时拉低 SLEEP 线，经 300 ms 消抖确认后立即切断加热并清空
  控制器历史，避免反复进出架子导致积分累积
- 传感器失效保护：热电偶开路、短路或读数过期（>0.6 s）时无条件关闭加热

层级二：Agent 主动巡检

- 自定义 Skill 在用户开始作业时主动建议设置定时巡检
- 通过设备端 cron 在预定时刻推送提醒
- 与层级一的区别：硬保护解决"已离架"，主动巡检解决"仍在架外但人已离开"

#### 3.3.3 openvela 系统能力运用

| 能力域 | 落地情况 | 使用组件 |
|---|---|---|
| 图形 | ✅ 深度使用 | LVGL 9.1、LTDC 显示控制器、帧缓冲、FT5x06 触摸 |
| AI | ✅ 使用 | ai_agent 框架、以太网链路、Skill 机制 |
| 多媒体 | ❌ 未涉及 | — |

**对 openvela 的改进建议**（基于实际开发中遇到的问题）：

1. **触摸驱动补全**：ft5x06 字符驱动未实现 NuttX 触摸框架所需的 `TSIOC_GETMAXPOINTS` 接口，
   导致 LVGL 无法取得触点信息。已迁移至触摸框架并提交上游 PR。
2. **显存与堆的地址规划**：板级定义中 SDRAM 被整体注册为系统堆，与 LTDC 帧缓冲基址重叠，
   任何遍历堆的操作都会崩溃。建议在板级模板中对显示内存与系统堆做显式隔离。
3. **HTTP 流式解析**：chunked 编码响应的读取循环仅依赖 `Content-Length` 判断结束，在
   keep-alive 连接上会阻塞至服务端超时。
4. **计时基准**：框架使用墙上时钟测量耗时，而证书校验会触发系统时间跳变，导致误判超时；
   建议统一采用单调时钟。

---

### 3.4 系统实现

#### 3.4.1 软件/固件架构

```
┌─────────────────────────────────────────────────────────┐
│                   charging_station (4133 行)             │
├─────────────────────────────────────────────────────────┤
│  应用层                                                  │
│  ├─ charging_station_main.c   319 行  初始化 · 100ms 调度 │
│  └─ charging_station_ui.c    1457 行  LVGL 三页界面      │
├─────────────────────────────────────────────────────────┤
│  驱动层                                                  │
│  ├─ iron.c                    565 行  MAX6675·PID·PWM    │
│  ├─ ina226.c                  424 行  2 路功率计量       │
│  ├─ sw3538.c                  318 行  4 路快充寄存器      │
│  └─ cs_i2c_sw.c               241 行  软件 I2C 传输层    │
├─────────────────────────────────────────────────────────┤
│  系统层（openvela / NuttX）                              │
│  LVGL 9.1 · i2c_bitbang · ft5x06 · LTDC · 帧缓冲 · 以太网 │
└─────────────────────────────────────────────────────────┘
```

分层原则：驱动层不依赖 UI，UI 只暴露 setter/getter。UI 是数值的唯一权威——主循环每 100 ms
反向读取目标温度、加热开关与 PID 参数并下发驱动层，避免双份状态。

#### 3.4.2 数据流与关键流程

正向（采样 → 显示）——各模块按不同周期上报：

| 周期 | 动作 | 设计考虑 |
|---|---|---|
| 100 ms | 热电偶读取（每 3 拍一次）+ PID + PWM | 避开 MAX6675 的 220 ms 转换期 |
| 100 ms | 快充通道读取（每次只读一路） | 见下方说明 |
| 500 ms | 功率监视器读取（2 颗） | 单次约 2 ms，可合并 |
| 5 s | 重探启动时缺席的设备 | 支持热插拔式恢复 |

关键设计——轮询拆分：四路快充各需 4 次 ADC 转换（每次等待 5 ms 锁存），一次读完约 92 ms，
超过 100 ms 定时器周期，会阻塞 LVGL 主循环导致触摸迟滞。改为每个 tick 只读一路后，单次占用
降至约 23 ms，四拍完成一轮扫描。

反向（用户 → 执行）：UI 事件只改 UI 自身状态，主循环统一同步至驱动层，形成单向数据流。

```
LVGL 定时器 (100ms)
   ├─► cs_ui_data_tick()          刷新界面文本
   ├─► 从 UI 读意图 ──► iron_set_*()   目标温度 / 加热开关 / PID
   ├─► iron_tick()               采样 → PID → PWM 输出
   ├─► sw3538_poll_channel(n)    读第 n 路快充 → UI 卡片
   └─► 每 5 拍: ina226_poll()    → 顶部输入功率 / 烙铁功率

LVGL 输入设备 (20ms)
   └─► 触摸事件 ──► 修改 UI 状态（下一轮被主循环取走）
```

#### 3.4.3 硬件设计与适配

**接线（Arduino 排针）**：

| 引脚 | MCU | 功能 | 方向 |
|---|---|---|---|
| D2 | PG3 | 烙铁休眠检测 | 输入（低有效，内部上拉） |
| D3 | PA6 | 加热 PWM | 输出 → NPN → MOSFET |
| D4 | PK1 | TCA9548A SCL | 输出（开漏，软件 I2C） |
| D5 | PA8 | TCA9548A SDA | 输出（开漏，软件 I2C） |
| D6 | PE6 | MAX6675 SO | 输入 |
| D7 | PI8 | MAX6675 CS | 输出 |
| D8 | PE3 | MAX6675 SCK | 输出 |

**总线拓扑**：

```
软件 I2C (D4/D5) ──► TCA9548A (0x70)
                       ├─ CH0~CH3 ──► SW3538 ×4 (0x3C) ──► 4×Type-C + 4×Type-A
                       ├─ CH4      ──► INA226 整机输入 (2.5 mΩ)
                       └─ CH5      ──► INA226 加热丝   (5.0 mΩ)

硬件 I2C4 (D14/D15) ──► FT5x06 触摸屏（独占，不被复用）
```

**是否完成驱动开发：是。** 共三项：

**① FT5x06 触摸驱动迁移**（nuttx 上游）

原字符驱动未实现 NuttX 触摸框架所需的 `TSIOC_GETMAXPOINTS` 接口，LVGL 因此无法取得触点。
迁移至 `touch_register()` 框架并补全接口，已提交上游 PR。

**② STM32H7 以太网 PHY 链路轮询**（nuttx 上游，+229 行）

本板未将 PHY 中断引出至 MCU，原驱动仅在 up 时读取一次链路状态，导致开机后插入的网线永远无法
被识别。新增基于 LPWORK 的周期性轮询（500 ms，2 次采样去抖），链路变化时发布 carrier 状态；
同时在启动时主动使能自协商，使无网线开机也能就绪。另处理了 MII_CRS/COL 引脚与 QSPI bank2
的复用冲突（QSPI-XIP 构建下需让出该复用）。

**③ 三个自研硬件驱动**

- `sw3538.c` — 四路快充芯片寄存器驱动（含 ADC 通道选择、协议解析、NTC 温度换算）
- `ina226.c` — 功率监视器驱动（整数校准、缺席重探、失败降级）
- `iron.c` — 热电偶位翻转 SPI + PID + 软件 PWM + 休眠检测

**适配难点与解法**：硬件 I2C4 已被触摸屏独占，而四颗快充芯片地址固定为 0x3C 必须经多路复用器
隔离。解法是自研软件 I2C 传输层（复用 NuttX `i2c_bitbang` 引擎），将多路复用器迁至独立总线，
使触摸屏保持硬件总线独占。

#### 3.4.4 应用/交互端设计

480×272 触控界面，三页签：

| 页 | 内容 |
|---|---|
| Charger | 顶部整机输入功率横幅；2×2 通道卡片，每卡显示通道号、快充协议徽章、芯片温度、USB-C/USB-A 两路的电压/电流/功率 |
| Iron | 左：大字号温度、目标温度滑块、±步进、加热开关；右：功率圆盘（PID 占空比 + 实测功率）、4 个温度预设 |
| Info | 时间、日期、天气、环境温湿度 |

界面采用 1:1 的 HTML 还原预览（`ui_preview.html`）随代码入库，便于评审在无硬件条件下查看布局。

#### 3.4.5 自定义 Skill

位置：`app/charging_station/skills/soldering-session.md`（运行时应置于 `/data/ai_agent/skills/`）

| 触发 | 行为 |
|---|---|
| 用户提及焊接、烙铁、温度 | 从长期记忆读取常用温度并给出建议 |
| 用户设定常用温度 | 写入长期记忆，跨会话保留 |
| 用户开始焊接作业 | 主动建议设置定时巡检，经 cron 在预定时刻推送 |
| 用户结束作业 | 提示放回烙铁架、列出待办提醒、更新偏好 |

使用的内置工具：`read_file`、`write_file`、`cron_add`、`cron_list`、`cron_remove`。

**说明**：开发板在验证阶段损坏，该 Skill 未在硬件上实际执行过，报告所列为设计定义。

---

<!--
待补章节：
  1、信息表
  3.5 系统测试与结果分析
  3.6 AI-Native 开发说明
  3.7 总结与展望
-->
