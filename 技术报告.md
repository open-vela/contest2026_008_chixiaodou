# 2026 首届 openvela AI 硬件开发者大赛 · 技术报告

## 1、信息表

| 项目 | 内容 |
|---|---|
| 作品名称 | openvela桌面充电站 |
| 队伍名称 | 赤小豆 |
| 团队分工 | 谢怡彬：全栈 |
| 选题方向 | AI 硬件产品创新 |

## 2、摘要

桌面充电设备分散、状态不可见，焊接工具又缺乏统一入口。本项目基于 STM32H750B-DK 与 openvela 研制了兼顾多口快充与智能烙铁的一体化焊接工作站。硬件含 SW3538、INA226、MAX6675 与 JBC245。软件实现 LVGL 三页界面、快充驱动、功率计量与 PID 恒温控制；自研软件 I2C 承载 TCA9548A，解决同地址与硬件 I2C 占用两项约束。接入 ai_agent 与云端大模型实现自然语言交互，沉淀 Skill 1 个。实测单轮 AI 响应 2170 ms，公网丢包 0 %，固件 966700 B 占 2 MB Flash 的 46.10 %。

## 3、正文

### 3.1 绪论

#### （1）项目背景与问题定义

在电子工程师、硬件爱好者与维修从业者的典型工位上，往往同时存在两类设备：一是为手机、平板、笔记本供电的多口充电器，二是电烙铁等焊接工具。当前使用中存在四个具体痛点。

① 充电设备分散、状态不可见。用户需要多个充电头配多根数据线，桌面凌乱；且传统充电器只能以指示灯粗略表示“正在充电”，无法得知每个接口实时的电压、电流、功率与协商到的快充协议。

② 焊接工具缺乏状态管理。电烙铁长时间通电空烧存在安全隐患，用户离开工位后常常忘记断电，而传统焊台没有主动提醒机制。

③ 两类设备缺乏统一的交互入口。充电与焊接设备各自独立，用户需要在多个设备之间分别操作，无法用统一的自然语言入口同时查询与控制。

④ 焊接过程中双手被占用，调节温度不便。实际焊接时通常一手持烙铁、一手持元件或焊丝，此时若想调整烙铁温度，必须先放下工具去操作面板，既影响效率也存在烫伤风险。本作品当前以触摸屏提供温度调节入口，用户仍需放下工具操作；以语音直接调温是更彻底的解法，将作为后续演进方向（见 3.7 节）。

将上述设备整合到一块主控上，还意味着把多条独立的信号链并入同一套总线与同一块屏幕。而开发板既有的外设布局进一步收紧了可用资源——硬件 I2C 已被触摸屏占用，充电芯片又采用固定从机地址，整合过程因而需要在总线资源与器件寻址上重新规划。

#### （2）技术难点

① 快充芯片资料匮乏。该芯片面向方案商供货，不提供 SDK、示例工程与调试工具，原厂仅有一份简略的数据手册，在 openvela / NuttX 乃至 Linux 生态中均检索不到可参考的驱动实现。本项目只能以手册为唯一依据，从零实现寄存器读写（写保护解锁、ADC 通道选择、12 位拼接），并借助万用表与电子负载实测，反推手册未言明的时序细节，这是整个项目中耗时最多、也最无外力可借的环节。

② 多路复用器后多器件的时序与轮询预算。每一路快充读取都需多次 ADC 转换，每次需等待约 5 ms 锁存；若在一次调度中读完所有通道，累计耗时接近 100 ms 调度周期，会阻塞 LVGL 主循环、导致触摸迟滞。

③ 功率调制的量化精度。JBC245 烙铁额定 84 W，而空烧恒温仅需约 6 %~18 % 的占空比。低分辨率 PWM 在该区间只有两档可用，加热器会在“完全断供”与“严重欠供”之间跳变，形成低频极限环而无法稳定恒温。

④ 实时性与资源约束。系统堆总量约 926 KB，需在有限内存下同时保证界面流畅、控制实时与 AI 会话可用。

#### （3）创新点

① 产品形态创新 —— 把“多口快充 + 智能烙铁 + 主动安全提醒”整合为一台桌面工作站，并以 AI 作为统一交互入口。市面上将充电与焊接两类设备整合、且以自然语言统一操作的一体化产品较少。

② 自然语言交互的设备化落地 —— 通过 openvela ai_agent 框架与云端大模型，用户可直接用自然语言查询各充电口状态、询问焊接工艺，而非在触摸屏上逐级点选，实测单轮对话响应 2170 ms。

③ 主动式安全提醒 —— 沉淀自定义 Skill 与定时巡检机制，主动提醒用户检查烙铁，把“用户记得”变成“设备提醒”，针对烙铁空烧这一真实安全隐患。该能力的设计与框架支持均已就位，受本版固件未划分可写用户分区所限，运行时加载尚未启用，属清晰的后续演进方向。

④ 双 I2C 总线架构 —— 在硬件 I2C 被触摸屏独占的前提下，自研软件 I2C 传输层承载多路复用器，用软件成本换取总线资源，在不干扰触摸屏的条件下解决多片同地址器件的共存问题。

⑤ 对 openvela 上游的反馈 —— 开发中定位的触摸驱动缺陷已完成迁移修复并提交上游（PR #374），另整理出显存规划、HTTP 流式解析、计时基准三项改进建议，具备社区贡献价值。

### 3.2 系统方案设计

#### （1）系统总体架构

系统由端侧控制器、外接模拟前端与云端大模型三部分组成。端侧以 STM32H750B-DK 为主控，向上承载 LVGL 触控界面与主应用，向下通过 GPIO 与软件 I2C 总线连接多路复用器，再分别寻址各路快充芯片与两路功率监视器；温控回路则以位翻转 SPI 直连热电偶与加热驱动。云端仅承担自然语言理解，经由板载以太网 PHY 与 DeepSeek 服务通信。

数据流为单向分层结构：驱动层按各自周期采集硬件状态并上报，主应用每 100 ms 统一刷新界面；用户操作只修改界面状态，由主循环反向读取后下发驱动层，界面是全部可变状态的唯一权威。

#### （2）方案论证与选型

本作品的主控选用 STM32H750B-DK。该板为 Cortex-M7 内核，配合 8 MB SDRAM 与 LTDC 显示控制器，可同时承载 480×272 的 LVGL 界面渲染与实时温控闭环，无需在显示效果与控制精度之间取舍；板载以太网 PHY 为 ai_agent 提供了原生网络通道，无需额外扩展模块即可接入云端大模型；同时保留充足的 Arduino 排针，便于外接快充、功率计量与温控模块。板载硬件 I2C4 已被触摸屏占用，且快充芯片地址固定为 0x3C，这两点约束直接决定了后续在总线选择与器件寻址上的设计走向。

在若干关键决策点上，方案的取舍如下表所示。

| 决策点 | 备选方案 | 取舍与理由 |
|---|---|---|
| 总线方案 | A：分时复用硬件 I2C4 | 与触摸屏争用同一总线，触摸响应受采集阻塞影响，否决 |
|  | B：软件 I2C + TCA9548A | 选用。软件 I2C 独占 GPIO，触摸屏保持硬件总线独占，多路复用器解决同地址冲突 |
| AI 落位 | A：端侧推理模型 | 系统堆约 940 KB，量化小模型亦无余量，否决 |
|  | B：云端大模型 | 选用。端侧负责感知与执行，云端负责语义理解，安全闭环不受网络影响 |
| 温度调制 | A：10 档 PWM（1 s 周期） | 恒温工作区仅两档可用，产生极限环，否决 |
|  | B：20 档 PWM（2 s 周期） | 选用。分辨率提升至 5 %，覆盖 5/10/15/20 % 四档，恒温稳定 |

端侧与云端的职责划分遵循一条原则：安全相关的闭环全部留在端侧，云端只负责增强。这一划分的出发点是，充电与温控属于用户正在依赖的基础功能，其可用性不能取决于网络状况。因此断网时设备退化为一台功能完整、但没有对话能力的充电站，而非不可用设备。

| 功能层 | 运行位置 | 断网时表现 |
|---|---|---|
| 快充通道管理 | 端侧 | 不受影响 |
| 烙铁温控（PID + PWM） | 端侧 | 不受影响 |
| 触控界面与状态显示 | 端侧 | 不受影响 |
| AI 对话 | 云端 LLM | 不可用 |
| Skill 定时提醒（沉淀能力） | 端侧触发 | 设计为端侧调度，不依赖网络 |

#### （3）关键模块设计

① 软件 I2C 传输层与通道调度。复用 NuttX 内核既有的 i2c_bitbang 引擎，将 STM32H7 的两根 GPIO 构造成一条完整 I2C 总线，并在其上挂接 TCA9548A 八通道多路复用器。传输层将“选择通道”与“访问从机”两个动作封装进同一把互斥锁，保证每次事务都在正确通道上完成，避免多路复用器状态被并发访问打乱。这样，多颗同地址快充芯片得以共存于一条总线。

② 温度闭环模块。由热电偶读取、PID 运算与软件 PWM 输出三部分构成，以固定节拍驱动，并内置采样新鲜度检查与超温判定作为失效保护，详见 3.3 节。

③ 数据采集与调度模块。将各路快充的读取拆分为每拍一路，使单次占用从约 92 ms 降至约 23 ms，避免阻塞界面线程；功率计量按 500 ms、缺席设备按 5 s 重探。

④ AI Agent 模块。基于 openvela 的 ai_agent 框架，通过以太网接入云端大模型，并以自定义 Skill 的形式注入焊接领域知识与主动提醒逻辑。

### 3.3 核心算法与技术原理

#### （1）AI 算法实现

端侧：本作品未使用端侧推理模型。这一选择基于需求本身——充电管理与温度控制均为确定性闭环算法（状态量明确、控制律可解析），引入神经网络既无必要，也会占用本就紧张的算力与内存预算。端侧承担感知与执行，云端承担语义理解与意图解析，形成明确的职责边界。

云端：设备通过板载 LAN8740A 以太网 PHY 接入网络，由 ai_agent 框架完成 HTTPS 链路与对话管理。客户端基于 openvela 树内既有的 MiMo 示例改造为通用 OpenAI 兼容客户端，接口采用 POST /v1/chat/completions，模型名为 deepseek-chat；鉴权凭据（API Key）存放于设备端数据目录，由框架在发起请求时注入 Authorization 请求头；传输层安全由 mbedTLS 承担，采用默认的 16 KB 输入/输出内容缓冲。

链路层工作中定位并修复了三处框架缺陷：其一，显存与系统堆地址重叠导致任何遍历堆的操作立即崩溃；其二，HTTP chunked 编码响应的读取循环仅依赖 Content-Length 判断结束，在 keep-alive 连接上会持续阻塞至服务端超时；其三，调用耗时的看门狗使用墙上时钟计时，而 TLS 证书校验会触发系统时间跳变，导致误判超时。三者互为因果、连锁暴露，须依次修复。

| 缺陷 | 位置 | 现象 | 修复方式 |
|---|---|---|---|
| 显存/堆地址重叠 | nuttx 板级 board.h | 遍历堆即 panic | 不再将 SDRAM 整体注册为堆，显存与堆显式隔离 |
| chunked 读取不退出 | ai_agent vela_tls.c | 每次调用耗时 61910 ms 后超时 | 新增按块长度判定终止块的完成检测，在读取循环内检查 |
| 看门狗用墙上时钟 | ai_agent agent_loop.c | 时钟跳变后必然判超时 | 改用 CLOCK_MONOTONIC 单调时钟并加溢出保护 |

#### （2）关键机制设计

① 温度闭环控制。控制律为位置式 PID，经输出限幅后映射到 20 档软件 PWM 驱动加热器。

采样节拍：MAX6675 单次转换时间为典型 0.17 s、最大 0.22 s，故每 3 个调度拍（300 ms）读取一次，按最坏情况仍留有约 36 % 的余量。该器件为 12 位输出、分辨率 0.25 ℃、量程 0 ℃~+1024 ℃，SPI 兼容。PID 的积分时间常数同步按 0.3 s 计算。温度超过 500 ℃ 判为异常（远在传感器量程之内），立即切断加热并将传感器标记为失效；采样年龄超过 0.6 s 同样无条件切断。

抗积分饱和（条件积分）：当输出已达限幅且误差方向不变时停止积分累加，避免退饱和时出现长时间超调。

微分抑制（斜率限幅）：采样周期 300 ms、分辨率 0.25 ℃，意味着单次量化跳变就会在差商中产生约 0.83 ℃/s 的噪声，直接微分会把测量噪声放大到输出上。因此对实测变化率限幅至 ±8 ℃/s（约为该量化噪声的十倍），而非对输出项限幅。

参数口径说明：界面暴露给用户的 P/I/D 与控制器实际使用的增益之间存在固定缩放（P×0.2、I×0.4、D×0.2），目的是让界面上的数字更符合直觉。报告与代码注释中出现的增益均需标明口径。

② PWM 量化误差分析与档位加密。JBC245 烙铁额定功率 84 W（24 V × 3.5 A），而空烧恒温（不进行焊接）时仅需约 5~15 W 维持，对应占空比 6 %~18 %。10 档 PWM 的分辨率为 10 %，意味着该区间只能取到两个离散值：

| PID 输出 | 10 档下的实际占空比 | 20 档下的实际占空比 |
|---|---|---|
| 1 % ~ 4 % | 0 %（完全断供） | 0 % / 5 % |
| 5 % ~ 9 % | 10 % | 5 % / 10 % |
| 10 % ~ 14 % | 10 % | 10 % / 15 % |
| 15 % ~ 19 % | 20 % | 15 % / 20 % |

温度到达设定值后 PID 输出回落至该区间，加热器在“断供”与“欠供”之间跳变，形成低频极限环，温度持续小幅振荡而无法稳定。解决方法是把 PWM 档位由 10 档加密至 20 档（周期 1 s → 2 s，分辨率 5 %），并以四舍五入替代截断取整；改进后该工作区间有 5 % / 10 % / 15 % / 20 % 四档可用，恒温得以稳定。周期延长是合理的：JBC245 烙铁头的热时间常数在数十秒量级（经验估计，未实测），2 s 的调制周期远小于其热惯性，功率脉动不会反映到温度上。

③ 安全联动机制。分为两个层级。

层级一为端侧硬保护，不依赖任何外部条件：休眠检测——JBC245 手柄放入烙铁架时拉低 SLEEP 线，经 300 ms 消抖确认后立即切断加热并清空控制器历史，避免反复进出架子导致积分累积；传感器失效保护——热电偶开路（MAX6675 的 D2 开路标志位置位）、数据帧无效（D15/D1 位异常）或读数过期（>0.6 s）时无条件关闭加热；超温保护——温度超过 500 ℃ 立即断热。

层级二为 Agent 主动巡检：自定义 Skill 在用户开始作业时主动建议设置定时巡检，并通过设备端 cron 在预定时刻推送提醒。与层级一的分工在于，硬保护解决“手柄已归架”的场景，主动巡检解决“手柄仍在架外、但人已离开”的场景。

④ 采样调度。每路快充各需 4 次 ADC 转换（每次等待约 5 ms 锁存），一次性读完约 92 ms，几乎占满 100 ms 调度周期，会阻塞 LVGL 主循环导致触摸迟滞。改为每个调度拍只读一路后，单次占用降至约 23 ms，四拍完成一轮扫描，界面响应不受影响。

#### （3）openvela 系统能力的深度运用

| 能力域 | 落地情况 | 使用的 openvela / NuttX 组件 |
|---|---|---|
| 图形 | 深度使用 | LVGL 9.1、LTDC 显示控制器、帧缓冲 fb、FT5x06 触摸框架 |
| AI | 使用 | ai_agent 框架、以太网链路、自定义 Skill 机制 |
| 多媒体 | 未涉及 | — |

基于实际开发中遇到的问题，对 openvela 提出以下改进建议：

① 触摸驱动接口补全。ft5x06 原字符驱动未实现 NuttX 触摸框架所需的 TSIOC_GETMAXPOINTS 接口，LVGL 无法取得触点信息，需将驱动迁移至 touch_register() 框架。建议在同类输入驱动的模板中一并补齐该接口。

② 显存与堆的地址规划。板级定义将 SDRAM 整体注册为系统堆，与 LTDC 帧缓冲基址完全重叠，任何遍历堆的操作都会崩溃。建议在板级模板中对显示内存与系统堆做显式隔离，避免使用者踩坑。

③ HTTP 流式解析。chunked 编码响应的读取循环仅依赖 Content-Length 判断结束，在 keep-alive 连接上会阻塞至服务端超时。建议在通用 HTTP 客户端中统一处理 chunked 终止块。

④ 计时基准。框架使用墙上时钟测量耗时，而证书校验会触发系统时间跳变，导致误判超时。建议统一采用单调时钟（CLOCK_MONOTONIC）作为超时与看门狗的计时基准。

### 3.4 系统实现

#### （1）软件 / 固件架构

分层原则：驱动层不依赖界面，界面只暴露 setter/getter。用户可设置的状态由界面唯一持有——主循环每 100 ms 反向读取目标温度、加热开关与 PID 参数并下发驱动层，避免同一状态存在两份副本。

固件以 NuttX 应用（builtin）形式集成，经 defconfig 配置项启用后由启动脚本自动拉起；应用源码通过 repo manifest 的 linkfile 软链入构建树，无需改动上游仓库的目录结构。

#### （2）数据流与关键流程设计

各模块按不同周期上报，周期性如下表。

| 周期 | 动作 | 设计考虑 |
|---|---|---|
| 100 ms | 热电偶读取（每 3 拍一次）+ PID + PWM | 避开 MAX6675 的 220 ms 转换期 |
| 100 ms | 快充通道读取（每次只读一路） | 单次约 23 ms，避免阻塞界面线程 |
| 500 ms | 功率监视器读取（2 颗） | 单次约 2 ms，可合并 |
| 5 s | 重探启动时缺席的功率监视器 | 支持热插拔式恢复 |
| 500 ms | 以太网链路状态轮询（驱动层） | PHY 中断未引出，改用轮询 + 去抖 |

反向（用户 → 执行）：界面事件只修改界面自身状态，主循环统一同步至驱动层，形成单向数据流，避免驱动与界面互相写入造成状态不一致。

#### （3）硬件设计与适配

外接模块通过 Arduino 排针接入，引脚分配如下表。

| 引脚 | MCU | 功能 | 方向 |
|---|---|---|---|
| D2 | PG3 | 烙铁休眠检测 | 输入（低有效，内部上拉） |
| D3 | PA6 | 加热 PWM | 输出 → NPN → MOSFET |
| D4 | PK1 | TCA9548A SCL | 输出（开漏，软件 I2C） |
| D5 | PA8 | TCA9548A SDA | 输出（开漏，软件 I2C） |
| D6 | PE6 | MAX6675 SO | 输入 |
| D7 | PI8 | MAX6675 CS | 输出 |
| D8 | PE3 | MAX6675 SCK | 输出 |

总线拓扑：软件 I2C 总线（D4/D5）下挂 TCA9548A 多路复用器，其 CH0~CH3 预留给 SW3538 快充芯片（地址 0x3C），当前实装 CH0~CH2 三路，CH3 因快充模块与板载网口存在结构干涉而空出，CH4、CH5 分别接整机输入与加热丝两路 INA226 功率监视器（分流电阻 2.5 mΩ / 5.0 mΩ）；硬件 I2C4 由触摸屏独占，不被复用。

是否完成驱动开发：是。具体包括三项工作。

① FT5x06 触摸驱动迁移至 NuttX 触摸框架（nuttx 上游贡献）。这是本项目硬件适配中投入最大的一处工作，与以太网链路轮询同属需要改动内核驱动的适配。

问题与根因：驱动加载后设备节点 /dev/input0 已存在，但 LVGL 始终判定触摸不可用，界面提示 no touchscreen。串口输出给出了关键线索——LVGL 在创建设备时报出 unsupported maxpoint 0。根因是 ft5x06 原驱动采用字符设备实现（注册 file_operations，通过 read/poll 与等待信号量交互），只提供了数据通路，并未实现 NuttX 触摸框架所要求的 TSIOC_GETMAXPOINTS 接口；而 LVGL 的触摸适配层正是通过该接口读取最大触点数，取到 0 便直接放弃该设备。这也解释了为何设备节点存在、read 也能读到数据，触摸却始终无法工作。

解决方案：将驱动整体迁移到 NuttX 标准触摸框架。删除字符设备实现（file_operations、open/close/read/poll、等待信号量及文件描述符管理，合计约 425 行），改为以 touch_register() 注册触摸下半部，bringup / shutdown / ioctl 三个入口改为 touch_lowerhalf_s 回调，触点数据经 touch_event() 上报。核心改动示意如下。

/* 修正前：字符设备实现，只提供 read/poll，未实现触摸框架的 *          TSIOC_GETMAXPOINTS 接口 —— LVGL 取不到 maxpoint，判定设备不可用 */static const struct file_operations g_ft5x06_fops ={  .open = ft5x06_open,  .read = ft5x06_read,  .poll = ft5x06_poll,};register_driver(devpath, &g_ft5x06_fops, 0666, priv); /* 修正后：迁移为 NuttX 标准触摸框架下半部 */touch_register(&priv->lower, devpath, 0);   /* 注册并对外提供框架接口 */touch_event(&priv->lower, &sample);         /* 上报触点采样 */

附带修正：迁移过程中一并修复了原驱动的一处旧缺陷——单点识别分支中 Y 轴判定误用了 X 轴阈值（应为 CONFIG_FT5X06_THRESHY，原代码写成了 CONFIG_FT5X06_THRESHX），该缺陷会使纵向的有效拖动被误判为无位移而直接丢弃。同时将轮询周期由 50 ms / 200 ms 收紧至 20 ms / 100 ms，触摸响应更为及时。

验证与上游贡献：修复后串口由 unsupported maxpoint 0 变为 open success, maxpoint 1，触摸采样可正常上报坐标，界面恢复交互。该改动已提交至 nuttx 上游仓库（提交 3e33af7995f，PR #374），是本作品对 openvela 生态的直接回馈。

② STM32H7 以太网 PHY 链路轮询（内核驱动改动，移植自参考实现）。本板未将 PHY 中断引出至 MCU，原驱动仅在接口 up 时读取一次链路状态，导致开机后插入的网线永远无法被识别。解决方案移植自社区参考实现并做了两处必要适配：其一，将编译开关 BOARD_ETH_PHY_POLL 改为无条件定义（原实现将其包在 QSPI 启动宏下，本构建无该符号，会导致整段轮询被编译掉）；其二，片内 Flash 启动下不定义 BOARD_ETH_MII_NO_CRS_COL，避免误让出 PHY 的 CRS/COL 引脚复用。在其基础上实现了基于工作队列的周期性轮询（500 ms，2 次采样去抖），链路变化时发布 carrier 状态；同时在启动时主动使能自协商，使无网线开机也能正常就绪。

③ 三个自研硬件驱动。sw3538.c——四路通道的快充芯片寄存器驱动（当前实装三路）：按数据手册实现 I2C 写保护解锁（Reg0x10 依次写入 0x20 / 0x40 / 0x80，写操作前必需）、ADC 通道选择与 12 位数据拼接、快充协议解析与 NTC 温度换算，并支持缺席通道的热插拔重探；ina226.c——功率监视器驱动，含整数校准、缺席重探与失败降级；iron.c——热电偶位翻转 SPI 读取、PID 运算、软件 PWM 输出与休眠检测。

适配难点与解法：硬件 I2C4 已被触摸屏独占，而快充芯片地址固定为 0x3C，必须经多路复用器隔离。解法是自研软件 I2C 传输层（复用 NuttX i2c_bitbang 引擎），将多路复用器迁至独立总线，使触摸屏保持硬件总线独占。

实测验证：在单芯片直连 I2C 的早期调试阶段，接入 PD 快充负载后读到输出电压约 20.0 V（20088 ~ 20220 mV）、Type-A 口电流最高 3295 mA、协议识别为 PD，与 20 V 档位一致，空载时输出维持 5100 mV。这组数据验证了 ADC 通道选择、12 位拼接与协议解析的正确性；经 TCA9548A 的多路读取因开发板损坏未完成上板验证（见 3.5 节）。

关键 BOM：STM32H750B-DK 开发板 ×1；TCA9548A 八通道 I2C 多路复用器 ×1；SW3538 快充模块 ×3（每路含 1×Type-C + 1×Type-A；设计支持 4 路，CH3 因与板载网口结构干涉未安装）；INA226 功率监视模块 ×2（分流电阻 2.5 mΩ / 5.0 mΩ）；MAX6675 热电偶数字转换模块 ×1；JBC245 烙铁手柄与烙铁架 ×1；加热驱动 NPN + MOSFET 模块 ×1。

#### （4）应用 / 交互端设计

480×272 触控界面，采用三页签结构。

| 页面 | 内容 |
|---|---|
| Charger | 顶部整机输入功率横幅；2×2 通道卡片，每卡显示通道号、快充协议徽章、NTC 温度，以及该通道 Type-C / Type-A 两口的电压、电流与功率 |
| Iron | 左侧为大字号实时温度、目标温度滑块、±步进按钮与加热开关；右侧为功率圆盘（显示 PID 占空比与实测功率）与 4 个温度预设（300 / 350 / 380 / 420 ℃） |
| Info | 时间、日期与环境信息展示 |

界面另提供一份 1:1 的 HTML 还原预览（ui_preview.html）随代码入库，其坐标、颜色与字号均按实现值还原，便于评审在无硬件条件下查看布局。需要说明的是，Info 页的天气与环境温湿度为界面演示用的固定占位数据，本项目未接入相应传感器。

#### （5）自定义 Skill

Skill 定义文件为 app/charging_station/skills/soldering-session.md，已按 ai_agent 运行时要求的格式沉淀（标题加描述段的 Markdown，部署至设备端 /data/ai_agent/skills/ 目录）。触发场景与行为如下表。

| 触发场景 | 行为 |
|---|---|
| 用户提及焊接、烙铁、温度 | 从长期记忆读取常用温度并给出建议；无记录时建议 350 ℃ 并先询问 |
| 用户开始焊接作业 | 主动建议设置定时巡检，并经设备端 cron 在预定时刻推送提醒 |
| 用户结束作业 | 提示将手柄放回烙铁架、列出待办提醒、按实际用温更新记忆偏好 |

Skill 声明使用 read_file、cron_add、cron_list、cron_remove 与 get_current_time 等内置工具。出于安全考虑，该 Skill 不包含任何直接驱动加热器的命令——温度只能通过触摸屏设置，AI 层只能读取与建议，不能写入执行器。

运行时说明：ai_agent 框架具备完整的 Skill 加载能力——构建提示词时会自动汇总 /data/ai_agent/skills/ 下的 Skill 并注入上下文，Skill 所依赖的 read_file、cron_add、cron_list、cron_remove、get_current_time 等工具也均已实现并注册。本版固件的根文件系统为只读，未划分可写的用户分区，/data 无法作为可写目录挂载，因此 Skill 的运行时加载在当前固件配置下暂不具备。这一限制来自文件系统布局而非软件设计，且与存储容量无关（板载 2 MB Flash 仅占用 46.10%）：Skill 定义、工具链与框架支持均已就位，后续为开发板扩展 SPI Flash 或 SD 卡等可写存储后，Skill 与长期记忆功能即可启用并持久化。

### 3.5 系统测试与结果分析

#### （1）测试环境

| 项目 | 配置 |
|---|---|
| 主控板 | STM32H750B-DK（Cortex-M7，2 MB Flash，8 MB SDRAM，480×272 LTDC 屏） |
| 外接模块 | TCA9548A ×1、SW3538 快充模块 ×3、INA226 ×2、MAX6675 ×1、JBC245 手柄 ×1 |
| 网络链路 | 板载 RJ45 → 上位机 USB 网卡（NetworkManager 共享模式）→ 外网 |
| 系统版本 | openvela（NuttX 内核），LVGL 9.1，mbedTLS |
| 调试方式 | 串口控制台（NSH） |

说明：开发板在验证阶段损坏，因此本节的量化数据仅限于板卡损坏前已完成并记录的测试项；其余功能仅给出实现状态说明，不作量化断言。

#### （2）功能测试

| 测试项 | 测试方法 | 结果 | 备注 |
|---|---|---|---|
| 系统启动与界面显示 | 上电观察串口与屏幕 | 通过 | 三页签界面正常呈现 |
| 触摸交互 | 触摸滑动页签、操作滑块/按钮 | 通过 | FT5x06 触摸框架迁移后 maxpoint=1，触点上报正常 |
| 多路快充状态读取 | 对照硬件逐路读取并显示 | 未验证 | SW3538 驱动与 TCA9548A 多路寻址已实现；开发板在验证阶段损坏，未完成上板验证 |
| 功率计量 | 读取两路 INA226 数据 | 未验证 | INA226 驱动与两路分流计量已实现；开发板在验证阶段损坏，未完成上板验证 |
| 烙铁温度闭环 | 设定目标温度并观察加热与稳定过程 | 未验证 | 热电偶驱动、PID 与软件 PWM 已实现；开发板在验证阶段损坏，未完成上板验证 |
| 休眠检测 | 手柄放入/取出烙铁架 | 未验证 | SLEEP 线消抖与加热切断逻辑已实现；开发板在验证阶段损坏，未完成上板验证 |
| DHCP 获取地址 | 观察启动日志与地址配置 | 通过 | 自动获取 IPv4 地址 10.42.0.75 |
| 以太网公网连通性 | ping 公网 IP 223.5.5.5 | 通过 | 10 发 10 收，丢包 0 %，RTT 平均 55 ms |
| DNS 域名解析 | ping www.baidu.com | 通过 | 域名正确解析为 183.240.99.224，10 发 9 收 |
| 网线热插拔 | 无网线开机，运行中插入网线 | 通过 | 开机不阻塞；插线后链路自动恢复，ping 10 发 10 收 |
| HTTPS / TLS 链路 | 调用公网 HTTPS 接口 | 通过 | TLSv1.2 握手成功，HTTP 200 |
| AI 对话 | 在 NSH 命令行启动 ai_agent 并输入对话指令 | 通过 | 端侧 Agent 与云端大模型链路正常，单轮响应 2170 ms |
| 自定义 Skill | 触发焊接会话 Skill | 已沉淀，待存储扩展 | Skill 定义与框架工具链均已就位；固件当前未划分可写分区，运行时加载留待具备可写存储后启用 |

#### （2.1）串口实测记录

下列输出为开发板串口控制台的原始记录（nsh> 为设备端 NuttX Shell 提示符），未作改写。

① 网络连通性 —— 板卡经 DHCP 获取地址后，公网 IP 与域名均可正常访问；网关地址因 DHCP 未下发有效值而不可达，但转发路径正常，不影响公网访问。

nsh> ping 10.55.0.1PING 10.55.0.1 56 bytes of dataNo response from 10.55.0.1: icmp_seq=0 time=1000 ms...9 packets transmitted, 0 received, 100% packet loss, time 9170 ms nsh> ping 223.5.5.5PING 223.5.5.5 56 bytes of data56 bytes from 223.5.5.5: icmp_seq=0 time=50.0 ms56 bytes from 223.5.5.5: icmp_seq=1 time=60.0 ms...10 packets transmitted, 10 received, 0% packet loss, time 10100 msrtt min/avg/max/mdev = 40.000/55.000/70.000/8.062 ms nsh> ping www.baidu.comPING 183.240.99.224 56 bytes of data56 bytes from 183.240.99.224: icmp_seq=0 time=100.0 ms56 bytes from 183.240.99.224: icmp_seq=1 time=50.0 ms56 bytes from 183.240.99.224: icmp_seq=2 time=50.0 msNo response from 183.240.99.224: icmp_seq=3 time=1000 msWARNING: Received after timeout56 bytes from 183.240.99.224: icmp_seq=4 time=430.0 ms...10 packets transmitted, 9 received, 10% packet loss, time 10110 msrtt min/avg/max/mdev = 40.000/103.333/430.000/32.035 ms

② 网线热插拔 —— 无网线开机不阻塞启动流程，运行中插入网线后链路自动恢复并重新获取地址，丢包率为 0 %。

nsh> ping www.baidu.com              （无网线开机后，运行中插入网线）PING 111.45.11.5 56 bytes of data56 bytes from 111.45.11.5: icmp_seq=0 time=40.0 ms56 bytes from 111.45.11.5: icmp_seq=1 time=240.0 ms...10 packets transmitted, 10 received, 0% packet loss, time 10100 msrtt min/avg/max/mdev = 40.000/90.000/240.000/56.213 ms

③ AI 对话链路 —— 设备端 Agent 经以太网调用云端大模型，单轮对话端到端响应 2170 ms。

[netmgr] Network connected: 10.42.0.75[agent] Agent loop started, free heap: 706496[llm] LLM config updated atomically: api.deepseek.com/v1/chat/completions[trace:0000008d35c3b13a] BEGIN chat=console chan=cli[vela_tls] Handshake OK: TLSv1.2 / TLS-ECDHE-RSA-WITH-AES-256-GCM-SHA384[llm] Response: 453 bytes text, 0 tool calls, finish=end_turn[trace:0000008d35c3b13a] iter=0 tool=(none) latency=2170ms llm=ok backend=0[agent] [Agent]: 你好！我是你的 AI 助手，很高兴见到你

④ 修复前对照 —— 下表第三项所述的 chunked 响应缺陷会导致每次调用被看门狗判为超时（61910 ms），修复后降至 2170 ms，改善约 28 倍。

[llm] Response: 185 bytes text, 0 tool calls, finish=end_turn[agent] LLM watchdog: call took 61910 ms (limit 60s), treating as timeout[trace:69a2c67e514cbea4] END status=timeout iters=1 tools=0 llm_ms=61910[agent] [Agent]: 请求超时，LLM 响应时间过长。

⑤ 触摸驱动修复前后对照 —— 修复前 LVGL 因取不到 maxpoint 而放弃设备；迁移至触摸框架后成功取得 maxpoint=1，触摸恢复。

[LVGL] [User]   (22.760, +10)  lv_nuttx_touchscreen_create: touchscreen /dev/input0 opening[LVGL] [Error]  (22.770, +10)  lv_nuttx_touchscreen_create: touchscreen /dev/input0 unsupported maxpoint 0Charging Station: display res=480x272Charging Station: no touchscreen (indev NULL)

[LVGL] [User]   (34.490, +10)  lv_nuttx_touchscreen_create: touchscreen /dev/input0 opening[LVGL] [User]   (34.500, +10)  lv_nuttx_touchscreen_create: touchscreen /dev/input0 open success, maxpoint 1Charging Station: display res=480x272Charging Station: touchscreen registeredCharging Station: entering poll loop

#### （3）性能测试

下表区分实测值与设计值：实测值来自板卡损坏前的实际测量与记录，设计值为依据代码与器件手册推导、尚未在硬件上复测的理论值。

| 指标 | 数值 | 类型 | 说明 |
|---|---|---|---|
| AI 对话响应时延 | 2170 ms / 2340 ms | 实测 | 两轮独立对话的端到端耗时（含 TLS 握手） |
| AI 时延（缺陷修复前） | 61910 ms | 实测 | chunked 响应读取缺陷导致被 60 s 看门狗判超时；修复后降至 2170 ms，改善约 28 倍 |
| HTTPS 握手结果 | TLSv1.2 / ECDHE-RSA-AES-256-GCM-SHA384 | 实测 | mbedTLS，内容缓冲 16 KB |
| 固件体积（基础版） | 584820 B / 27.89 % | 实测 | 含界面、快充、功率计量与温控，不含 AI 链路 |
| 固件体积（含 AI 链路） | 966700 B / 46.10 % | 实测 | 追加 mbedTLS + webclient + cJSON + 大模型客户端 |
| 可用系统堆 | 约 940 KB | 实测 | 显存与堆隔离后，DTCM + SRAM1/2/3/4 + AXI |
| 运行期空闲堆 | 706496 B | 实测 | AI Agent 主循环启动后的可用堆 |
| 公网 RTT | 平均 55 ms（40~70） | 实测 | ping 223.5.5.5，10 发 10 收 |
| 界面刷新周期 | 100 ms | 设计值 | LVGL 定时器调度周期 |
| 单路快充读取耗时 | 约 23 ms | 设计值 | 含 4 次 ADC 转换，每次等待约 5 ms |
| 快充整轮扫描 | 约 92 ms（分 4 拍） | 设计值 | 拆分前单次占用约 92 ms，会阻塞界面 |
| 热电偶采样周期 | 300 ms | 设计值 | 相对 220 ms 转换时间留约 36 % 余量 |
| PWM 分辨率 | 20 档 / 2 s 周期（5 %） | 设计值 | 由 10 档加密而来 |
| 以太网链路轮询 | 500 ms（2 次去抖 ≈ 1 s） | 设计值 | 驱动层工作队列周期 |
| 任务栈占用 | ai_agent 32720 B / UI 32712 B | 实测 | 由系统任务转储读出 |

#### （4）可靠性与稳定性测试

网络链路可靠性：公网 IP 连通性测试 10 发 10 收、丢包率 0 %，RTT 平均 55 ms；域名解析正确（www.baidu.com 解析为 183.240.99.224），10 发 9 收、丢包率 10 %，其中一次出现超时后到达，属链路抖动而非功能缺陷。网关地址因 DHCP 未下发有效值而不可达，但转发路径正常，不影响公网访问。无网线开机不阻塞启动流程，运行中插入网线后链路自动恢复，恢复后测试 10 发 10 收、丢包率 0 %。

内存与资源稳定性：显存与系统堆地址重叠缺陷修复后，遍历堆的操作可正常执行，系统堆总量 926136 B、空闲 845736 B；AI Agent 主循环启动后仍保有 706496 B 空闲堆。此前该缺陷会导致任何堆遍历立即崩溃，属阻塞性缺陷。

故障发现与修复的量化对照：AI 对话链路存在一处响应读取缺陷（详见 3.3 节），表现为每次调用耗时 61910 ms 并被看门狗判为超时；定位并修复后，同一调用降至 2170 ms，改善约 28 倍，且随后连续多轮对话均稳定在 2.2~2.3 s 区间。

安全兜底方案（针对加热回路的漏检场景）：采用三层防护——第一层为采样新鲜度检查，采样年龄超过 0.6 s 即无条件切断加热，用于覆盖传感器无响应或读数失效的情形；第二层为传感器有效性判定，热电偶开路、数据帧无效或读数超过 500 ℃ 立即断热并标记失效；第三层为休眠检测，手柄放入烙铁架经 300 ms 消抖后切断加热。三层均在端侧独立生效，不依赖网络或云端。

异常恢复：网线掉线后网络层清除既有地址信息，链路恢复时自动重新应用地址策略，无需重启设备。

未完成的测试项：开发板在验证阶段的硬件调试中损坏，属硬件故障，与本作品固件无关——本作品最终固件体积 966700 B，仅占板载 2 MB Flash 的 46.10%，容量留有充足余量。受此影响，长时间连续运行（内存与帧率波动）、温控稳态精度、功率计量误差、以及多路快充的满载并发等测试未能执行。本版固件未划分可写用户分区，Skill 的运行时加载未在本版启用，其端到端效果留待具备可写存储后验证。上述内容已在报告中如实标注，后续更换硬件并完善存储布局后将优先补齐。

### 3.6 AI-Native 开发说明

| 指标 | 数据 |
|---|---|
| AI Coding 代码占比 | 约 90 %。口径：以本项目自研源码为分母（应用核心代码 3447 行，即 6 个 .c 源文件；若计入全部头文件与板级定义为 4265 行），不含 openvela 上游既有代码。开发过程中累计执行 82 次文件写入、518 次代码编辑，覆盖 21 个自研文件；上游补丁与板级改动亦以 AI 辅助为主要生产方式。 |
| 使用的 AI 工具 | Claude Code（终端 CLI，使用其 Skill、子代理与任务编排机制）；会话日志另覆盖 opencode / codex / kiro 等工具的历史记录位。 |
| MCP 工具使用情况 | 本项目开发过程未调用 MCP 工具。openvela 工作区已支持通过 MCP 接入构建与调试能力，但本项目的板级操作均通过串口控制台与本地构建完成。 |
| Skills 使用与新增情况 | 开发期间调用 Skill 共 10 次：openvela-build 5 次，kconfig-tweak、nuttx-driver-development、submit-pr、contest-log-collector、update-config 各 1 次。新增沉淀 Skill 1 个：焊接会话 Skill（soldering-session），面向焊接作业的偏好记忆与定时巡检。 |
| Token 使用总量 | 约 20.4 亿（输入约 1.96 亿 + 输出约 343 万，另计缓存读取约 18.4 亿）。统计口径为本项目开发会话，覆盖 25 天、累计 4909 次模型调用；该数字来自本地会话记录，非云服务控制台计费口径。开发期间多次重装系统，另有部分会话记录未留存，估计 6 亿以上 token 未纳入本统计。若按会话日志原始行累加为 46.9 亿，其中约 2.3 万条属同一调用在流式过程中被重复落盘，已去重，不计入用量。 |

#### 补充：AI 工具对开发效率的提升，以及遇到的主要问题与解决方式。

效率提升体现在三个方面。其一是板级 bring-up 的排查速度：串口日志的解读、寄存器与引脚复用的冲突分析、PHY 与总线时序的定位，这些原本需要反复查阅数据手册与参考实现的工作被显著压缩，本项目在 25 天内完成了从环境搭建到联网、AI 链路打通的完整流程。其二是驱动代码的生成与迭代：三个自研驱动（sw3538 / ina226 / iron）均在 AI 辅助下完成，并同步补齐了边界检查、失效保护与降级逻辑，这类“附带质量”在人工编写时往往被推迟到最后。其三是大型工作区中的定位能力：openvela 工作区规模达 42 GB、含数百个上游仓库，AI 工具能够快速定位到相关驱动、配置项与参考实现，避免了大量盲搜。

遇到的问题主要有三类。第一类是硬件事实需要反复核实：AI 对芯片行为的假设并非总是成立，例如快充芯片 ADC 是否需要显式使能、NTC 量程是否覆盖目标区间，都必须回到数据手册逐条确认，不能以代码“看起来对”为准。第二类是引脚与配置的连带影响：改动一个引脚或使能一个配置项，可能引发其他外设的复用冲突，本项目就出现过引脚分配变动导致系统异常的情况，最终依靠对比备份固件与逐项回退定位。第三类是结论的可靠性：AI 给出的判断需要被当作假设而非结论对待，本项目在排查阶段出现过将硬件故障误判为软件缺陷的情形。

应对方式归结为一条工作纪律：所有 AI 产出都必须落到可复现的证据上——代码须经编译验证与上板实测，寄存器与时序须回查数据手册，故障结论须由串口日志或对照实验支撑。本报告 3.5 节区分“实测值”与“设计值”、并保留串口原始记录，正是这一纪律的体现。

### 3.7 总结与展望

#### （1）成果总结

本项目完成了一台基于 openvela 的桌面焊接工作站原型样机，将多口快充、功率计量与智能烙铁整合于一块主控，并以 AI 作为统一交互入口。

硬件方面：以 STM32H750B-DK 为主控，外接多路复用器、快充模块、功率监视器、热电偶转换模块与烙铁加热驱动电路，通过 Arduino 排针完成互连。

系统方面：在 STM32H750B-DK 上部署 openvela / NuttX，落地图形与 AI 两项核心能力。

软件方面：应用核心代码 3447 行，实现 LVGL 三页触控界面、快充寄存器驱动、功率计量、热电偶与 PID 恒温控制，以及自研软件 I2C 传输层与多路复用寻址。实测固件 966700 B，占 2 MB Flash 的 46.10 %，运行期空闲堆 706496 B。

AI 方面：接入 ai_agent 框架与云端大模型，实现自然语言对话式交互，实测单轮响应 2170 ms，并沉淀自定义 Skill 1 个。

社区回馈：向 openvela 上游提交触摸驱动迁移修复（PR #374），另整理出显存规划、HTTP 流式解析、计时基准三项改进建议。

#### （2）应用前景与商业价值

目标受众：电子工程师、硬件创客、维修从业者与数码爱好者，以 20~40 岁、一二线城市、工位上同时具备充电与焊接需求的人群为主。

商业模式：以整机销售为主，可搭配不同功率档位的快充模块与不同型号烙铁头选配；主动安全提醒等功能可作为增值服务。

规模化潜力：所选器件均为成熟通用器件，电路为单板设计，BOM 成本可控，适合中小批量生产。

差异化：市面上少见“多口快充 + 智能烙铁 + AI 交互”的一体化工位设备，切中特定人群的实际需求。

#### （3）不足与未来工作

不足：一是开发板在验证阶段硬件损坏，长时间连续运行、温控稳态精度、功率计量误差与满载并发等测试未能完成；二是本版固件未划分可写用户分区，Skill 与运行时配置无法持久化；三是多媒体（音频与语音）方向尚未涉及。

未来工作：其一，更换硬件后补齐 3.5 节所列的量化测试项。其二，为开发板扩展 SPI Flash 或 SD 卡等可写存储，启用 Skill 与长期记忆的持久化，使主动安全提醒能力真正落地。其三，扩展语音控制：针对“焊接时双手被占用、无法腾出手调温”这一痛点，实现以语音直接调整烙铁温度，并加入语音唤醒与 TTS 播报，统一使用大赛指定的唤醒词。其四，利用空闲的 Arduino 排针接入环境感知（温湿度、电流互感器等），并借助板载以太网与 ai_agent 的通道能力接入智能家居生态。
